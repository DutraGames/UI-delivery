import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container: {
        marginTop: 15,
        marginHorizontal: 10,
        backgroundColor: '#f00',
        borderRadius: 10,
        width: 200,
        height: 200,
    },
    img:{
        width: '100%',
        height: '100%',
        resizeMode: 'center'
    },
  });

  export default styles